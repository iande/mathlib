package org.idecc.math.xmetrics;

public class CheckeredMetric extends org.idecc.math.MetricCalculator
{
	public double calculateMetric(org.idecc.math.Point c)
	{
		return Math.max(Math.abs(Math.cos(c.x)),Math.abs(Math.sin(c.y)));
	}
}