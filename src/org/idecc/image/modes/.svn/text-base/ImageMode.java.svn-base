package org.idecc.image.modes;

public interface ImageMode {
    /**
     * Combines the aRGB channels of two pixels.  The resulting channels are
     * stored in <code>top</code>.
     */
    public void performPixelMode(short[] top, short[] bottom);
}